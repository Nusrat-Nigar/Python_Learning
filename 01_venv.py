# v1 - pkg1(pandas)
# v2 - pkg2(numpy)
# v3 - pkg3(scipy)
# ".\env2\Scripts\Activate.ps1" (to activate env2)
# "pip install -r requirements.txt" (used to install the all pkgs which is mentioned in requirements.txt file )

import moviepy