# Ordered, immutable(unchangable) collections of items
my_tuple = (3,2,4,5,8)
print(my_tuple)

print(my_tuple[3])



# my_tuple[3] = 32 # We can't change due to immutability behaviour

# in order to create the single element tuple we need to create like this :
tuple = (3, )
print(tuple)